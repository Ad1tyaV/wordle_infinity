class ResponsiveSize {
  double height;
  double width;
  double margin;
  double fontSize;

  ResponsiveSize(this.height, this.width, this.margin, this.fontSize);

  double getHeight() => height;

  double getWidth() => width;

  double getMargin() => margin;

  double getFontSize() => fontSize;
}